const randomNumber = Math.random(); // produces random number between 0 (including) and 1 (excluding)

if (randomNumber > 0.7) {
  alert('greater');
}

const arNumbers = [2, 7, 9, 7];

for (let el in arNumbers) {
  console.log(el);
}
